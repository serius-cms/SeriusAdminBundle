
{% extends base_template %}

{% block title %}
    {% if admin.id(object) is not null %}
        {{ "title_edit"|trans({'%name%': admin.toString(object) }, 'SonataAdminBundle') }}
    {% else %}
        {{ "title_create"|trans({}, 'SonataAdminBundle') }}
    {% endif %}
{% endblock%}

{#{% block page_title %}#}
    {#{{ parent() }} -#}
    {#{% if admin.id(object) is not null %}#}
        {#{{ "title_edit"|trans({'%name%': admin.toString(object) }, 'SonataAdminBundle') }}#}
    {#{% else %}#}
        {#{{ "title_create"|trans({}, 'SonataAdminBundle') }}#}
    {#{% endif %}#}
{#{% endblock %}#}

{% block actions %}
    <div class="sonata-actions">
        {% include '@SonataAdmin/Button/show_button.html.twig' %}
        {% include '@SonataAdmin/Button/history_button.html.twig' %}
        {% include '@SonataAdmin/Button/acl_button.html.twig' %}
        {% if admin.id(object) is not null %}
            {% include '@SeriusAdmin/Button/create_button.html.twig' %}
        {% endif %}
        {% include '@SeriusAdmin/Button/list_button.html.twig' %}
    </div>
{% endblock %}

{% block side_menu %}{{ knp_menu_render(admin.sidemenu(action), {'currentClass' : 'active', 'style': 'list'}) }}{% endblock %}

{% use '@SeriusAdmin/CRUD/base_edit_form.html.twig' with form as parentForm %}

{% block form %}
    {{ block('parentForm') }}
{% endblock %}
